import Stemmer from '@nlpjs/core/src/stemmer';
export declare class StemmerBg implements Stemmer {
    stem(tokens: string[]): string[];
    private stemToken;
    private isStopWord;
    private getRoot;
}
