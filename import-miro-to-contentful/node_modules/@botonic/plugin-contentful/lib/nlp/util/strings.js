"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.replaceAll = exports.trim = exports.rtrim = exports.ltrim = void 0;
const escape_string_regexp_1 = __importDefault(require("escape-string-regexp"));
function ltrim(str, chars) {
    while (str.length && chars.includes(str[0])) {
        str = str.slice(1);
    }
    return str;
}
exports.ltrim = ltrim;
function rtrim(str, chars) {
    while (str.length && chars.includes(str[str.length - 1])) {
        str = str.slice(0, str.length - 1);
    }
    return str;
}
exports.rtrim = rtrim;
function trim(str, chars) {
    str = rtrim(str, chars);
    return ltrim(str, chars);
}
exports.trim = trim;
/**
 * String.replaceAll only available in esnext
 */
function replaceAll(haystack, oldStr, newStr) {
    oldStr = escape_string_regexp_1.default(oldStr);
    return haystack.replace(new RegExp(oldStr, 'g'), newStr);
}
exports.replaceAll = replaceAll;
//# sourceMappingURL=strings.js.map