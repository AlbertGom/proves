import * as contentful from 'contentful';
import * as cms from '../../cms';
import { Context } from '../../cms';
import { TopContentDelivery } from '../content-delivery';
import { DeliveryApi } from '../delivery-api';
import { CommonEntryFields } from '../delivery-utils';
export declare class PayloadDelivery extends TopContentDelivery {
    constructor(delivery: DeliveryApi, resumeErrors: boolean);
    payload(id: string, context: Context): Promise<cms.Payload>;
    fromEntry(entry: contentful.Entry<PayloadFields>, context: Context): cms.Payload;
}
export interface PayloadFields extends CommonEntryFields {
    payload: string;
}
