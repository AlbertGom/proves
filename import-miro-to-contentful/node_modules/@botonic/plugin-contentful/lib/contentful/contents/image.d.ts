import * as contentful from 'contentful';
import * as cms from '../../cms';
import { Context } from '../../cms';
import { DeliveryApi } from '../delivery-api';
import { CommonEntryFields } from '../delivery-utils';
import { DeliveryWithFollowUp } from './follow-up';
export declare class ImageDelivery extends DeliveryWithFollowUp {
    constructor(delivery: DeliveryApi, resumeErrors: boolean);
    image(id: string, context: Context): Promise<cms.Image>;
    fromEntry(entry: contentful.Entry<ImageFields>, context: cms.Context): Promise<cms.Image>;
}
export interface ImageFields extends CommonEntryFields {
    image: contentful.Asset;
}
