import * as cms from '../../cms';
import { ResourceDelivery } from '../content-delivery';
export declare class AssetDelivery extends ResourceDelivery {
    asset(id: string, context: cms.Context): Promise<cms.Asset>;
    private fromEntry;
    assets(context: cms.Context): Promise<cms.Asset[]>;
}
