import * as contentful from 'contentful';
import { Context } from '../../cms';
import * as cms from '../../cms';
import { TopContentDelivery } from '../content-delivery';
import { DeliveryApi } from '../delivery-api';
import { CommonEntryFields } from '../delivery-utils';
import { ScheduleDelivery, ScheduleFields } from './schedule';
export declare class QueueDelivery extends TopContentDelivery {
    private readonly schedule;
    static REFERENCES_INCLUDE: number;
    constructor(delivery: DeliveryApi, schedule: ScheduleDelivery, resumeErrors: boolean);
    queue(id: string, context: Context): Promise<cms.Queue>;
    fromEntry(entry: contentful.Entry<QueueFields>): cms.Queue;
}
export interface QueueFields extends CommonEntryFields {
    queue: string;
    schedule?: contentful.Entry<ScheduleFields>;
    handoffMessage?: string;
}
