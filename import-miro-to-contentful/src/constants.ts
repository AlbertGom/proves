import { MiroContentType } from "miro";

export const MIRO_CONTENT_TYPES: Record<string, string> = {
  SHAPE: "shape",
  LINK: "line",
};

export const ContentTypes: Record<string, MiroContentType> = {
  TEXT: "text",
  BUTTON: "button",
};
